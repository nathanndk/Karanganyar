const products = {
    createConfig: [
        {
            key: "name",
            label: "Nama Lengkap",
            type: "text",
            placeholder: "Masukkan Nama Lengkap",
        },
    ],
};

export default products;
